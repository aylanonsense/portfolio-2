{
  "name": "portfolio",
  "version": "0.0.1",
  "main": "server.js",
  "scripts": {
    "build": "npm run build:metadata && npm run build:grid && npm run build:spritesheets && npm run build:html",
    "build:html": "node scripts/build-html.js",
    "build:grid": "node scripts/build-grid.js",
    "build:metadata": "node scripts/build-metadata.js",
    "build:spritesheets": "node scripts/build-sprite-sheets.js"
  },
  "author": "bridgs",
  "dependencies": {
    "express": "^4.15.3"
  },
  "devDependencies": {
    "binpacking": "0.0.1",
    "canvas": "^1.6.5",
    "image-size": "^0.5.4",
    "load-json-file": "^2.0.0",
    "mustache": "^2.3.0",
    "write-json-file": "^2.1.0"
  }
}
